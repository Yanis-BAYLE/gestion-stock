spring:
  datasource:
    url: jdbc:mysql://localhost:3306/stock_db
    username: stock_user
    password: stock_password

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8080